import React from 'react';
import { Form, Input, Button, Checkbox } from 'antd';

function Login() {
    return (
        <div>
            <h1>Login</h1>
        </div>
    );
}
export default Login;